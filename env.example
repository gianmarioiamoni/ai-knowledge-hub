# ============================================================================
# AI Knowledge Hub - Environment Variables Template
# ============================================================================
# Copy this file to .env.local and fill in your actual values
# Never commit .env.local to version control!
# ============================================================================

# ----------------------------------------------------------------------------
# SUPABASE CONFIGURATION (Required)
# ----------------------------------------------------------------------------
# Get these from: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL="https://your-project-id.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-supabase-anon-key"
SUPABASE_SERVICE_ROLE_KEY="your-supabase-service-role-key"

# ----------------------------------------------------------------------------
# OPENAI CONFIGURATION (Required)
# ----------------------------------------------------------------------------
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-your-openai-api-key"

# ----------------------------------------------------------------------------
# EMAIL CONFIGURATION (Required - Choose ONE method)
# ----------------------------------------------------------------------------
# Option 1: Gmail (Recommended for development)
# Generate app password: https://myaccount.google.com/apppasswords
GMAIL_USER="your-email@gmail.com"
GMAIL_APP_PASSWORD="your-16-char-app-password"

# Option 2: SMTP Server (For production or custom email providers)
# SMTP_HOST="smtp.your-provider.com"
# SMTP_PORT="587"
# SMTP_USER="your-smtp-username"
# SMTP_PASSWORD="your-smtp-password"
# SMTP_FROM="noreply@your-domain.com"

# Admin email for notifications and system alerts
ADMIN_EMAIL="admin@your-domain.com"

# ----------------------------------------------------------------------------
# STRIPE CONFIGURATION (Required)
# ----------------------------------------------------------------------------
# Get these from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY="sk_test_your-stripe-secret-key"
STRIPE_WEBHOOK_SECRET="whsec_your-webhook-secret"

# Stripe Price IDs for subscription plans
# Create these in: https://dashboard.stripe.com/products
STRIPE_PRICE_SMB_MONTHLY="price_xxx_smb_monthly"
STRIPE_PRICE_SMB_ANNUAL="price_xxx_smb_annual"
STRIPE_PRICE_ENTERPRISE_MONTHLY="price_xxx_enterprise_monthly"
STRIPE_PRICE_ENTERPRISE_ANNUAL="price_xxx_enterprise_annual"

# ----------------------------------------------------------------------------
# CRON JOBS (Optional)
# ----------------------------------------------------------------------------
# Secret token to protect cron endpoints from unauthorized access
# Generate with: openssl rand -base64 32
CRON_SECRET="your-random-cron-secret-token"

# ----------------------------------------------------------------------------
# SUPER ADMIN CONFIGURATION (Optional - for initial setup)
# ----------------------------------------------------------------------------
# These credentials will be used to create the super admin account
# All three must be set together or all omitted
SUPERADMIN_EMAIL="superadmin@your-domain.com"
SUPERADMIN_PASSWORD="your-secure-password-min-8-chars"
SUPERADMIN_NAME="Super Admin"

# ----------------------------------------------------------------------------
# DEMO USER CONFIGURATION (Optional - for demo/testing)
# ----------------------------------------------------------------------------
# Credentials for the demo user account (if seed demo data is enabled)
# All three must be set together or all omitted
DEMO_USER_EMAIL="demo@your-domain.com"
DEMO_USER_PASSWORD="your-demo-password-min-8-chars"
DEMO_USER_NAME="Demo User"

# ============================================================================
# NOTES:
# ============================================================================
# 1. Required variables: Supabase, OpenAI, Email (Gmail OR SMTP), Stripe
# 2. Email: Must configure EITHER Gmail OR all SMTP_* variables
# 3. Admin notifications: ADMIN_EMAIL or SUPERADMIN_EMAIL must be set
# 4. Super Admin: All 3 variables (EMAIL, PASSWORD, NAME) must be set together
# 5. Demo User: All 3 variables (EMAIL, PASSWORD, NAME) must be set together
# 6. For production: Use environment-specific values in Vercel dashboard
# ============================================================================
